apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: argocd
  namespace: argocd
spec:
  generators:
  - clusters:
      selector:
          matchLabels:
              nl.vancalker.managedservices/argocd-install: 'true'
  template:
    metadata:
        name: '{{ name }}-argocd'
    spec:
        project: 'default'
        source:
            repoURL: 'https://gitlab.com/martijncalker/poc-argocd-kubernetes-applicationsets.git'
            targetRevision: '{{ metadata.labels.nl.vancalker.managedservices/argocd-install-version }}'
            path: '/applications/{{ metadata.labels.version }}/argocd-install'
        destination:
            server: '{{ server }}'
            namespace: 'argocd'
        syncPolicy:
            automated:
                prune: true
